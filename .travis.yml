language: ruby
before_script: "bundle exec rails db:setup"
env:
  - TESTSCRIPT="bin/travis-tests"
  - TESTSCRIPT="bundle exec mutant -r ./config/environment --use rspec ProjectPolicy -j 1"
script:
  - $TESTSCRIPT