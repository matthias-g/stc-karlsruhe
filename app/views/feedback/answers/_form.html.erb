<%= form_for(@feedback_answer) do |f| %>
  <% if @feedback_answer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@feedback_answer.errors.count, "error") %> prohibited this feedback_answer from being saved:</h2>

      <ul>
      <% @feedback_answer.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :survey_answer_id %><br>
    <%= f.number_field :survey_answer_id %>
  </div>
  <div class="field">
    <%= f.label :question_id %><br>
    <%= f.number_field :question_id %>
  </div>
  <div class="field">
    <%= f.label :text %><br>
    <%= f.text_area :text %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
