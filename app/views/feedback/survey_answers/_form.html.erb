<%= form_for(@feedback_survey_answer) do |f| %>
  <% if @feedback_survey_answer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@feedback_survey_answer.errors.count, "error") %> prohibited this feedback_survey_answer from being saved:</h2>

      <ul>
      <% @feedback_survey_answer.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :survey_id %><br>
    <%= f.number_field :survey_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
