
<% flash.each do |msg_type, entry| %>
  <% (entry.kind_of?(Array) ? entry : [entry]).each do |msg| %>
    <div class="alert <%= bootstrap_alert_class(msg_type) %> alert-dismissible fade show" role="alert">
      <%= msg %>
      <button type="button" class="close" data-dismiss="alert" aria-label="Close">
        <span aria-hidden="true">&times;</span>
      </button>
    </div>
  <% end %>
<% end %>