<%= render '/pages/smallheader' -%>

<section>
  <h1><%= @page.title %></h1>
  <%= link_to t('page.action.edit'), edit_page_path(@page) %> |
  <%= link_to t('page.action.toOverview'), pages_path %>
</section>