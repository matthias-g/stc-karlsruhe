<%= form_for [refinery, :projects_admin, @type] do |f| -%>
  <%= render '/refinery/admin/error_messages',
              :object => @type,
              :include_object_name => true %>

  <div class='field'>
    <%= f.label :title -%>
    <%= f.text_field :title -%>
  </div>

  <%= render '/refinery/admin/form_actions', :f => f,
             :continue_editing => false,
             :delete_title => t('delete', :scope => 'refinery.projects.admin.projects.type'),
             :delete_confirmation => t('message', :scope => 'refinery.admin.delete', :title => @type.title) -%>
<% end -%>

<% content_for :javascripts do -%>
  <script>
    $(document).ready(function(){
      page_options.init(false, '', '');
    });
  </script>
<% end -%>
